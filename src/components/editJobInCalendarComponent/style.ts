import { makeStyles } from "@material-ui/core/styles";

const useStyleJobListTable = makeStyles({
  buttonGrp: {
    marginTop: "50px",
  },
});

const useStyleConfirmationDialog = makeStyles({
  button: {
    marginRight: "10px",
  },
  buttonWrapper: {
    marginRight: "auto",
  },
});
export { useStyleJobListTable, useStyleConfirmationDialog };
