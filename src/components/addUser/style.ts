import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles({
  form: {
    display: "flex",
    flexDirection: "column",
    width: "300px",
  },
  formElement: {
    marginTop: "20px",
  },
  inputLabel: {
    marginLeft: "10px",
  },
});

export { useStyles };
